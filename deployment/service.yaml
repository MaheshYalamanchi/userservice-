apiVersion: v1
kind: Service
metadata:
  name: aiservice
  annotations:
    service.beta.kubernetes.io/azure-load-balancer-internal: "true"
spec:
  selector:
    app: aiservice
  ports:
    - port: 3005
      targetPort: 3005
  type: LoadBalancer